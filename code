print("We're starting in...")

// Edit the range 1...3 in the loop to use stride() 🏁
for num in stride(from: 3, to: 0, by: -1 ) {
  print(num)
}

//from 3, 3-1 = 2, 2-1 = 1, 1-0 = 0(not printing 0)
//by: -1 means to DECREMENT cause we use a negative number

print("GO!")
